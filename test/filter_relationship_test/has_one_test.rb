require 'test_helper'

class HasOneTest < ActiveSupport::TestCase
  
  # Currently has_one is just and alias for has_many
  # Not Supported, works on has_many because of counter_cache
  # test "::filter :has_one => true" do
  # test "::filter :has_one => false"
  # test "::filter has_one_association with lambda" do
  # test "::filter on model and has_one_association"

end